version: '3.8'

services:
  dreamgf:
    build: .
    ports:
      - "10000:10000"
    environment:
      - DATABASE_URL=sqlite:///data/dreamgf.db
      - REDIS_URL=memory://
      - PORT=10000
    volumes:
      - ./data:/app/data
      - ./temp:/app/temp
    restart: unless-stopped
